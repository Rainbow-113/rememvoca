import 'package:flutter/cupertino.dart';

class playlist_screem extends StatelessWidget{
  const playlist_screem({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(child: Center(child: Text("plaulish"),),);
    throw UnimplementedError();
  }
}
